## 2026-08-01

### 补全 emoji 可插拔系统阶段 2：AI 层提示词注入
- `agent/agent.go`：`Config` 新增 `EmojiTheme` 字段；新增 `emojiThemeHint` 常量（结构化表达约束）；`New()` 中当 `EmojiTheme` 非空时自动追加提示词到 system prompt；`buildContext()` 透传 `EmojiTheme` 到 `ai.Context`
- `cmd/pg/chat.go`：创建 `agent.Config` 时传入 `EmojiTheme` 字段，打通完整透传链
- 透传链路：`cmd/pg` → `agent.Config` → `ai.Context.EmojiTheme`
- 未配置主题时行为完全不变，向后兼容

### my-openai provider 修复
- 修复 `stream_able` → `stream`：JSON 字段名错误导致本地 OpenAI 兼容模型不返回流式响应
- `OpenAIRuquest` 中 `MaxTokens`/`Temperature` 添加 `omitempty`，避免零值发送到 API

### max_tokens 默认值改进
- `chat.go`：MaxTokens 未配置时自动从 `prov.Models()` 获取模型最大值，不再需要用户手动配置
- `config.go`：`PiConfig.MaxTokens` 添加 `omitempty`，零值不写入配置文件
- `config_cmd.go`：`config init` 不设置 max_tokens，配置文件中以注释示例展示可选配置项（`base_url`/`system_prompt`/`max_tokens`/`temperature`/`max_steps`）
- `config_cmd.go`：`config set provider/model` 不再自动更新 MaxTokens，由运行时自动获取

### API key 错误提示优化
- `chat.go`：API key 未设置时的错误信息改为分条列出 3 种设置方式，每种附具体命令示例

### 日志落盘
- `main.go`：新增 `initLogging()`，程序启动时将日志输出重定向到 `logs/` 目录
- 使用 `lg.SetPath()` 一行完成 FileWriter 创建 + SetDefault + SetFrameWriter
- `.gitignore`：新增，忽略 `logs/`、编译产物、IDE 文件

### lg 库：LogNamePattern 链式文件名构建器
- `lg/writer.go`：新增 `LogNamePattern` 结构体，最小粒度链式组合
  - `.Module()` / `.Date(layout)` / `.Clock(layout)` / `.Timestamp()` / `.Char(s)` 最小片段
  - `.Build()` 收尾，自动追加 `.log` 后缀
- `lg/writer.go`：新增 `NewFileWriterWithLogName(dir, level, logName)`，支持动态文件名自动切分
- `lg/logger.go`：新增 `SetPath(dir, level, pattern, opts...)`，一步完成日志路径设置
- `lg/logname_test.go`：10 个测试覆盖所有组合模式 + 文件切换 + 自定义路径，全部通过

### 日志库扩展计划
- `docs/日志库扩展计划.md`：5 项可插拔扩展能力规划
  - LevelDir（默认）：自动按 debug/info/warn/error/fatal 分目录
  - RotateByInterval/RotateBySize：定时/定大小切割
  - Retention：定时清理过期日志
  - LevelPriority：高优先级日志出现时屏蔽低优先级

### lg 库：LevelDir 分级目录（LogOption 模式）
- `lg/logger.go`：新增 `LogOption` 类型和 `logConfig` 结构体，统一扩展入口
- `lg/logger.go`：新增 `WithLevelDir(level, dir)`，将指定级别日志输出到独立子目录
- `lg/logger.go`：`SetPath` 签名改为 `SetPath(dir, level, pattern, opts ...LogOption)`
  - 无 opts：单个 FileWriter，不分级
  - 有 WithLevelDir：MultiWriter 组合默认 FileWriter + 各子目录 FileWriter，按 level 过滤
- `lg/logger_test.go`：新增 3 个测试（不分级/分级/级别过滤），全部通过

### lg 库：LevelPriority 废弃
- `SetPath` 的 `level` 参数已覆盖级别过滤需求，无需额外实现
- `docs/日志库扩展计划.md`：LevelPriority 标记为已废弃

### lg 库：Retention 定时清理
- `lg/logger.go`：新增 `WithRetention(maxAge)`，保留时长内不过期，超过即删除
- `lg/logger.go`：新增 `startRetention(dir, maxAge)`，启动后台 goroutine，检查间隔 = min(maxAge, 1h)
- `lg/logger.go`：新增 `cleanDir(dir, maxAge)`，递归清理目录中超过 maxAge 的 .log 文件，不删非 .log 文件
- `lg/logger_test.go`：新增 3 个测试（清理旧文件/保留新文件/子目录清理），全部通过

### lg 库：RotateBySize / RotateByInterval
- `lg/logger.go`：新增 `WithRotateBySize(size)`，超过 size 字节时自动切新文件
- `lg/writer.go`：`FileWriter` 新增 `rotateSize`/`written`/`seq` 字段，`Write` 中累计字节数超阈值时递增序号并切换
- `lg/writer.go`：新增 `insertSeq(name, seq)`，在 `.log` 前插入 `.001` 等三位补零序号
- `lg/logger.go`：新增 `WithRotateByInterval(d)`，在 pattern 末尾自动追加日期+时钟
  - < 1小时 → `pg_2026-08-01_12-00-00.log`（秒精度）
  - >= 1小时 → `pg_2026-08-01_12-00.log`（分钟精度）
  - >= 1天 → `pg_2026-08-01.log`（天精度）
- `lg/logger.go`：新增 `intervalLayout(d)` 辅助函数
- 不依赖 goroutine，纯文件名策略：每次 Write 时 FileWriter 检测文件名变化自动切换
- 可与 RotateByInterval 组合使用
- `lg/logger_test.go`：新增 5 个测试（切换/序号格式/小时/天/分钟），全部通过

### 终端 UI 微调：去掉重复回显、Markdown 渲染、日志分级、光标修复、文案修正
- `lg/logger.go`：`Logger` 新增 `writerSrc *Logger` 字段；包级 `Module()` 创建子 Logger 时持有 `defaultLogger` 指针作为 writerSrc，log 时从其动态读取 writer；新增 `SetDefaultWriter(w)` 只修改 `defaultLogger.writer` 字段（保持子 Logger 引用有效），`SetPath` 改用 `SetDefaultWriter`；删除之前的 `writerPtr`/`effectiveWriter`/全局 `defaultWriterPtr` 复杂机制；修复 `SetPath` 分级配置：不再创建根目录汇总文件，避免日志同时出现在根目录和子目录
- `lg/logger_test.go`：保留 `TestModule_FollowsSetDefault` 验证子 Logger 跟随行为；`TestSetPath_WithLevelDir` 新增验证根目录无日志文件
- `cmd/pg/main.go`：`initLogging()` 改用 `WithLevelDir` 按级别分目录存储（info/error）；`"Pi Agent"` → `"Pi-Go Agent"`（system prompt、help）
- `tui/terminal.go`：新增 `StripANSI()` 函数；新增 `BgCard()` 函数（后删除）；修复光标定位：`fullRender`/`diffRender` 末尾改用 `CursorBack(len(line) - curCol)` 替代 `CursorForward(curCol)`，确保光标从文本末尾正确移到 curCol 位置；`renderLine` 光标位置字符反色显示（`\033[7m`）
- `tui/box.go`：保留 Unicode 框线工具（备用，未使用）
- `tui/markdown.go`：修复 `renderInline` 标题处理 bug，`### 标题` 之前未去掉 `### ` 前缀导致用户看到裸 Markdown；改为 `Bold(strings.TrimPrefix(trimmed, "### "))`
- `tui/chat.go`：`printWelcome()` 中 `"Pi Agent"` → `"Pi-Go Agent"`；`Run()` 中去掉 `renderUserBox`（LineEditor 已实时回显）；`renderAgentStream` 保持流式输出，每行实时输出，无背景色；工具调用/结果/tokens 缩进 2 空格对齐
- `tui/chat_test.go`：测试调用 `renderAgentBox` → `renderAgentStream`

### 构建、打包、发布流程
- `cmd/pg/main.go`：`version`/`commit`/`buildDate` 改为 `var`（支持 ldflags 注入）；新增 `printVersion()` 输出完整版本信息（含 go runtime 版本）
- `LICENSE`：MIT 许可证
- `.goreleaser.yaml`：goreleaser 配置，交叉编译 darwin/arm64 + windows/amd64，ldflags 注入版本号，自动打包 tar.gz/zip + checksums + changelog；Release 正文通过 `header_file` 引用 `version-log/` 文件
- `.github/workflows/release.yml`：Git Tag 触发 goreleaser，自动构建+发布 GitHub Release
- `install.sh`：重写为自动检测 OS/Arch，从 GitHub Releases 下载预编译二进制（无需 Go 环境）
- `.gitignore`：完善，增加 `dist/`、`*.exe`、`*.test`、OS 文件
- `pack.sh`：本地手动交叉编译打包脚本（不依赖 CI）
- `Makefile`：统一命令入口，按流程分组（编译打包发布/代码检查/版本管理/清理/帮助）
- `cmd/pg/` 拆分为 `main.go`（公共）+ `main_unix.go`（Unix 路由）+ `main_windows.go`（Windows 路由）+ `chat.go`（`!windows`），支持 Windows 交叉编译
- `tui/` 全部文件加 `//go:build !windows`，隔离 Unix 特定 `syscall` 依赖
- `version-log/1.0.0.md`：从 change-log 总结的发版说明，作为 GitHub Release 正文来源
- `docs/扩展计划/构建、打包、发布流程.md`：重写，补全所有模块命令/路径/步骤
- `README.md`：安装方式更新、架构图更新、Makefile 命令参考、贡献指南 + PR 流程 + SOP 示范模块

### CI lint 修复 + actions 升级
- `.github/workflows/ci.yml`：`golangci-lint-action` v6 → v7
- `.github/workflows/release.yml`：`goreleaser-action` v6 → v7
- 修复全部 27 个 golangci-lint 问题（errcheck 20 / unused 2 / gosimple 2 / ineffassign 1 / staticcheck 1）
- `ai/providers/anthropic/provider.go`：修复 `evtType` 逻辑 bug（`continue` 跳过后未赋值导致使用旧值）
- 涉及文件：`lg/`(7)、`cmd/pg/`(4)、`tui/`(4)、`ai/providers/`(5)、`agent/`(1)

### CI 对齐 go.mod 1.26.5
- `.github/workflows/ci.yml`：全部改用 `go-version-file: go.mod` 自动读取版本；去掉 1.23/1.26 矩阵，单版本即可；golangci-lint 从 `@latest` 改为固定 `v1.64`；去除 `golangci-lint-action`，改用 `go run` 直接运行
- `.github/workflows/release.yml`：同样改用 `go-version-file: go.mod`；goreleaser 从 `version: latest` 改为固定 `~> v2.8`；`actions/checkout` v4→v5，`actions/setup-go` v5→v6，消除 Node.js 20 弃用警告
- 修改成功，第一版发布成功