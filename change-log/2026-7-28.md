## 2026-07-28

### 项目初始化
- 初始化 go module: `github.com/Effortful-lion/pi-go`，Go 版本 1.26.5
- 创建项目骨架目录结构：`cmd/pi/`, `ai/`, `agent/`, `tool/`, `tui/`, `lg/`, `docs/`, `docs/learning/`
- 从 llmLib 参考项目引入 `lg` 日志库（Writer 接口 + Router + ConsoleWriter/FileWriter/MultiWriter）
- 创建 AGENTS.md 作为 CodeBuddy 开发指导文件
- 创建 CHANGELOG 目录结构 `change-log/`

### AI 层：核心类型定义
- 新增 `ai/types.go`：定义 Message/Role、ContentBlock/ContentBlockType、ToolDefinition/ToolCall、Context 基础类型
- 设计决策：Message 使用 flat struct + Role 字段，ContentBlock 用 flat struct + Type 字段，避免 Go interface union 泛滥

### AI 层：事件系统
- 新增 `ai/event.go`：定义 12 种 EventType（Start/TextStart/TextDelta/TextEnd/Thinking*/ToolCall*/Done/Error）、Event 结构体、Stream 通道类型（`<-chan Event`）、Usage 统计
- 设计决策：用 Go channel 作为流式传输机制，for-range 天然消费

### AI 层：Model 与 Provider 接口
- 新增 `ai/model.go`：ModelInfo 元信息结构体、Model 接口（Info + Chat）
- 新增 `ai/provider.go`：Provider 接口（ID/Name/Models/Model/Chat），Provider 同时是模型目录和聊天入口

### AI 层：OpenAI Provider 实现
- 新增 `ai/providers/openai/provider.go`：基于 `net/http` 纯标准库实现 OpenAI Chat Completions Streaming API
- SSE 解析 → Event 流映射（内联实现，无三方依赖）
- 支持文本增量（TextDelta）和工具调用增量（ToolCallStart/Delta/End）
- 处理 arguments 二次 JSON 编码问题（decodeArg 解码）
- 使用 `lg.Module` 记录关键日志
- 新增 `ai/providers/openai/provider_test.go`：mock HTTP server 测试，覆盖文本流/工具调用流/错误响应/模型不存在 4 个场景，全部通过（4 PASS）

### 设计文档归档
- 新增 `docs/ai-layer-design.md`：AI 层完整设计文档（架构分层、模块设计、关键决策、消息映射）

### Tool 接口定义
- 新增 `tool/tool.go`：定义 Tool 接口（Name/Definition/Execute），独立 `tool/` 包，最小契约原则
- 放在 tool 包而非 agent 包内，因为 tool 是独立的知识域

### Agent 运行时层：事件系统
- 新增 `agent/event.go`：定义 AgentEventType 枚举（StepStart/StepEnd/ToolCall/ToolResult/TextDelta/Done/Error）、Event 结构体、Stream 类型
- AgentEvent 比 ai.Event 更高层，带有运行时语义（Step 编号、Tool 进展）

### Agent 运行时层：核心循环
- 新增 `agent/agent.go`：Agent 结构体 + Run 方法，实现 LLM ↔ 工具调用自动循环
- 核心逻辑：用户消息入队 → 调用 LLM → 收集文本/工具调用 → 执行工具 → 注入结果 → 循环，直到 LLM 无工具调用或超限
- 对话历史 (`messages []ai.Message`) 内置管理，支持多轮对话
- goroutine + channel 异步架构，与 AI 层 Stream 模式一致
- 配置项：Provider, ModelID, SystemPrompt, Tools, MaxSteps, Temperature, MaxTokens
- 工具执行失败不中断循环，将错误信息作为 ToolResult 注入历史

### Agent 运行时层：测试
- 新增 `agent/agent_test.go`：mockProvider + mockTool 测试，覆盖 5 个场景全部通过
  - 纯文本对话、单次工具调用、多轮工具调用、maxSteps 超限、工具执行失败恢复
- 测试使用纯 mock，不依赖外部 API

### 设计文档归档
- 新增 `docs/agent-layer-design.md`：Agent 运行时层完整设计文档（架构分层、模块设计、循环逻辑、测试策略、关键差异）

### TUI 层：终端底层
- 新增 `tui/terminal.go`：原始终端控制 + ANSI escape 封装
- Raw Mode：通过 `syscall` + `termios` 实现（EnterRawMode/restore），支持 macOS
- 光标控制：CursorUp/Down/Forward/Back/MoveTo/Hide/Show（8 个函数）
- 屏幕控制：ClearLine/ClearLineFromCursor/ClearScreen/LineStart
- 颜色样式：Color 枚举（8 色+灰）、Style 结构体（Fg/Bold/Dim/Italic/Underline）、StyleText 函数
- 便捷样式：Bold/Dim/Red/Green/Blue/Cyan/Gray/Yellow 8 个快捷函数
- 行编辑：EditLine 函数，支持单行文本编辑（← → Home End Backspace Delete Enter Ctrl+C Ctrl+D Ctrl+← → 跳词）
- 键盘读取：readKey 解析单字符按键和 ANSI 转义序列

### TUI 层：交互式对话 UI
- 新增 `tui/chat.go`：ChatUI 结构体，封装 Agent 对话交互流程
- Run 方法：进入 raw mode → 欢迎信息 → 循环（提示符→读取输入→agent.Run→流式渲染）→ 退出后恢复终端
- 流式渲染策略：行式追加（非全屏重绘），EventTextDelta 直接写入终端实现打字效果
- 角色标记：💬 You（青色）、🤖 Assistant（绿色）、[调用工具]（灰色）、[工具结果]（灰色，100字截断）
- Usage 统计显示：≥1000 tokens 显示为 "1.2k tokens"，<1000 显示精确数字
- 错误处理：红色 ✖ + 错误信息

### TUI 层：测试
- 新增 `tui/terminal_test.go`：ANSI 输出验证，覆盖纯文本/加粗/红色/组合样式/默认色/零色/便捷函数 8 个场景，全部通过
- 新增 `tui/chat_test.go`：ChatUI 渲染测试，mockAgent 驱动，覆盖文本响应/工具调用/错误/Usage显示/truncate 5 个场景，全部通过
- 测试通过 stdout 重定向捕获输出验证

### 设计文档归档
- 新增 `docs/tui-layer-design.md`：TUI 层完整设计文档（与 TS 版差异、raw mode 设计、流式渲染策略、外观设计）

### CLI 产品入口
- 新增 `cmd/pi/main.go`：将 ai/agent/tui 三个库层组装为可执行产品 `pi`
- 命令行参数：`-provider`/`-model`/`-api-key`/`-base-url`/`-system-prompt`/`-max-steps`/`-temperature`/`-max-tokens`/`-version`
- 环境变量回退：`resolveFromEnv` 通过 `flag.Visit` 判断参数是否显式设置，未设置时从 `OPENAI_API_KEY`/`PI_MODEL` 等环境变量读取
- 参数优先级：命令行 > 环境变量 > 默认值
- 默认系统提示词：Pi Agent AI coding assistant
- 版本号：`0.1.0`
- 首版纯文本对话，不注册工具，纯 `flag` 标准库，无三方 CLI 框架

### 设计文档归档
- 新增 `docs/cli-entry-design.md`：CLI 产品入口设计文档（命令行参数、参数优先级、Agent 创建流程）

### 学习总结文档
- 新增 `docs/learning/01-日志库.md`：lg 日志库学习总结（Writer 接口、Router 路由、Logger 分层、Entry 结构化）
- 新增 `docs/learning/02-AI层-核心类型.md`：AI 层核心类型学习总结（Role/Message/ContentBlock/ToolDefinition/Context、Event 事件系统、Model/Provider 接口）
- 新增 `docs/learning/03-AI层-OpenAI实现.md`：OpenAI Provider 实现学习总结（SSE 协议、goroutine+channel 异步架构、SSE→Event 映射、arguments 二次解码、mock HTTP server 测试）
- 新增 `docs/learning/04-Tool接口.md`：Tool 接口学习总结（最小契约 3 方法、包独立性、ToolDefinition 设计哲学）
- 新增 `docs/learning/05-Agent运行时.md`：Agent 运行时学习总结（ReAct 循环、对话历史管理、两层事件系统、mock 测试策略）
- 新增 `docs/learning/06-终端UI.md`：终端 UI 学习总结（Raw Mode/termios、ANSI escape codes、EditLine 行编辑、ChatUI 流式渲染、行式追加 vs 全屏重绘）
- 新增 `docs/learning/07-CLI产品入口.md`：CLI 产品入口学习总结（胶水代码、flag 参数解析、环境变量回退、依赖注入组装）
- 7 篇学习总结按开发时序排列，面向零基础/后端转型人员，每篇包含：概念解释、设计思路、关键代码示例、关键决策、思想总结

### 扩展计划文档
- 新增 `docs/扩展计划/01-AI层扩展.md`：Anthropic/Gemini Provider（P0/P1）、Provider 注册与发现（P2）、流式优化/重试（P2）
- 新增 `docs/扩展计划/02-Agent运行时扩展.md`：Reset/Subscribe（P0）、Continue/ToolChoice/Parallel（P1）
- 新增 `docs/扩展计划/03-Tool系统扩展.md`：File & Shell Tool（P0）、Web Search/MCP 协议（P1）、安全框架（P2）
- 新增 `docs/扩展计划/04-TUI层扩展.md`：历史记录/多行输入（P0）、语法高亮/Markdown/清屏（P1）、文件粘贴/补全/导出（P2）
- 新增 `docs/扩展计划/05-CLI产品扩展.md`：配置文件（P0）、子命令/Session 持久化（P1）、多 Agent/HTTP Server/插件（P2）
- 5 篇扩展计划基于各层设计文档的"后续扩展方向"章节总结，按 P0→P1→P2 分级

### 扩展计划执行（17 项，排除 [待考虑] 标记项）

#### Agent 运行时扩展
- `agent.go`：新增 `Reset()` 清空对话历史保留 system prompt、`Continue(extraContext)` 不追加空 user 消息继续对话、`Messages()` 返回对话历史快照
- `Run()` 重构：抽取 `runLoop` 核心循环，`Run` 仅在 userInput 非空时追加消息，`Continue` 调用 `Run("")`
- `agent/session.go`：新增 Session 持久化（JSONL 格式），`SaveSession`/`LoadSession`/`ListSessions`/`DeleteSession`

#### Tool 系统扩展
- `tool/builtin/file.go`：File Tool 组（read_file/write_file/list_dir/search_file），单文件 250 行
  - read_file：分页读取（offset/limit），行号标注，最大读取大小 1MB
  - write_file：创建/覆盖写入，自动创建父目录
  - list_dir：列出目录，`d` 前缀标记目录
  - search_file：按通配符模式递归搜索
  - 安全：AllowedRoots 白名单限制 + 符号链接解析后再比较
- `tool/builtin/shell.go`：Shell Tool（shell），`command` + `cwd` 参数
  - AllowedCommands 白名单、BlockedPatterns 黑名单（默认: rm -rf/sudo/chmod 777/curl|sh/wget|sh）
  - 30s 超时，使用 `exec.CommandContext`
  - 命令失败返回 exit code + stdout/stderr

#### AI 层扩展
- `ai/providers/anthropic/provider.go`：Anthropic Claude Messages API（claude-sonnet-4-5/haiku-4-5）
  - system prompt 通过顶层 `system` 字段传递（非消息列表）
  - SSE 事件：message_start/content_block_start/delta/stop/message_delta/stop/ping
  - 工具调用通过 content_block（type=tool_use/input_json_delta）映射到 ai.Event
- `ai/providers/google/provider.go`：Google Gemini API（gemini-2.5-flash/pro）
  - API key 作为 query param，alt=sse 模式
  - systemInstruction 通过 parts 传递
  - 消息角色映射：assistant→model，tool→user+functionResponse
  - functionDeclarations 工具格式

#### TUI 层扩展
- `terminal.go`：EditLine → LineEditor 结构体重构
  - 多行输入：Alt+Enter 在光标处拆行为两行，←→↑↓ 多行导航，Backspace 行首合并
  - 历史记录：↑↓ 浏览，自动去重/裁剪（1000 条上限），浏览中间按普通键退出并恢复编辑内容
  - Ctrl+L 清屏并重绘
  - Tab 路径补全：自动完成文件路径，多候选取公共前缀
  - 新增便捷样式：Magenta/Italic/Underline
  - 兼容旧 API：EditLine 委托给 LineEditor
- `tui/highlight.go`：终端语法高亮（无三方依赖）
  - 支持 Go/Python/JS/Shell/JSON/YAML 6 种语言
  - tokenBasedHL 通用框架：关键词（紫色）、内置函数（青色）、字符串（绿色）、注释（灰色）
  - JSON 特殊处理：键名普通色、值字符串绿色、数字青蓝色
- `tui/markdown.go`：终端 Markdown 渲染
  - 支持：#标题、**粗体**、*斜体*、`行内代码`、```代码块```、- 列表、> 引用、| 表格|
  - 代码块自动检测语言并进行语法高亮
  - MarkdownLine 流式逐行渲染、RenderSimple 仅代码高亮
  - 提供 visualWidth/PadRight 辅助表格渲染
- `tui/chat.go` 重构：
  - 使用 LineEditor + 历史记录
  - 支持 /clear /reset /export /help 斜杠命令
  - ExportConversation：导出完整对话为 Markdown 文件

#### CLI 产品扩展
- `cmd/pi/main.go` 重构：命令路由器 `pi {chat|config|version|help}`
- `cmd/pi/chat.go`：chat 命令实现，支持多 Provider 切换（openai/anthropic/google）
- `cmd/pi/config.go`：PiConfig 结构体 + YAML 加载/保存，参数优先级：命令行 > 环境变量 > 配置 > 默认
- `cmd/pi/config_cmd.go`：`pi config init/show/set` 子命令
- 版本号升级：0.2.0

### 设计文档归档
- 新增 `docs/扩展计划执行计划.md`：完整执行计划（17 项扩展/3 阶段/文件变更汇总/风险分析）

### CLI 命令重命名：pi → pg
- CLI 命令从 `pi` 重命名为 `pg`，目录从 `cmd/pi/` 移至 `cmd/pg/`
- 所有帮助文本、README 中的命令示例同步更新
- **保持不变**：项目名 `pi-go`、Go module `github.com/Effortful-lion/pi-go`、配置路径 `~/.pi-go/`、品牌名 "Pi Agent"、环境变量前缀 `PI_`
- `cmd/pg/main.go`：品牌名修正 `PiGo Agent` → `Pi Agent`
- `cmd/pg/config.go`：曾错误改为 `~/.pg-go/`，已还原为 `~/.pi-go/` 和 `.pi-go.yaml`
- `README.md`：所有命令示例 `pi`→`pg`，同步安装脚本使用说明

### 安装脚本
- 新增 `quickstart.sh`：源码级别快速启动（检查 Go → `go install ./cmd/pg/` → PATH 配置 → 验证）
- 新增 `install.sh`：远程安装（`go install github.com/Effortful-lion/pi-go/cmd/pg@latest`）
- 新增 `uninstall.sh`：一键卸载（删除二进制 → 移除 shell 配置中 PATH 行 → 删除 `~/.pi-go/`）
- 修复脚本 `export PATH` 不生效问题：改用完整路径验证 + 提示 `source ~/.zshrc`
- 兼容 macOS/Linux 的 sed 差异

### 首次运行体验
- `cmd/pg/chat.go`：新增 `configFileExists()` 检查，无配置文件时提示 `pg config init`
- 错误提示中的配置路径统一为 `~/.pi-go/config.yaml`

### TUI 终端问题修复（进行中）
- `tui/terminal.go`：
  - 移除 `newState.Oflag &^= syscall.OPOST`：恢复输出处理，确保 `\n` → `\r\n`
  - `renderAll`：添加 `CursorHide()`/`CursorShow()` 保护渲染过程，末尾 `os.Stdout.Sync()` 强制刷写
  - `ReadLine`：移除冗余的顶层 `CursorShow()/CursorHide()`，改为 `renderAll` 内部管理
  - `readKey`：修复 `os.Stdin.Read` 错误被忽略问题（`_` → `err` 检查）
  - **已知问题**：输入字符仍不显示在终端上，待进一步排查
