## 2026-08-01

### 补全 emoji 可插拔系统阶段 2：AI 层提示词注入
- `agent/agent.go`：`Config` 新增 `EmojiTheme` 字段；新增 `emojiThemeHint` 常量（结构化表达约束）；`New()` 中当 `EmojiTheme` 非空时自动追加提示词到 system prompt；`buildContext()` 透传 `EmojiTheme` 到 `ai.Context`
- `cmd/pg/chat.go`：创建 `agent.Config` 时传入 `EmojiTheme` 字段，打通完整透传链
- 透传链路：`cmd/pg` → `agent.Config` → `ai.Context.EmojiTheme`
- 未配置主题时行为完全不变，向后兼容

### 终端 UI 微调：去掉重复回显、Markdown 渲染、日志分级、框线、文案修正
- `lg/logger.go`：`Logger` 新增 `writerSrc *Logger` 字段；包级 `Module()` 创建子 Logger 时持有 `defaultLogger` 指针作为 writerSrc，log 时从其动态读取 writer；新增 `SetDefaultWriter(w)` 只修改 `defaultLogger.writer` 字段（保持子 Logger 引用有效），`SetPath` 改用 `SetDefaultWriter`；删除之前的 `writerPtr`/`effectiveWriter`/全局 `defaultWriterPtr` 复杂机制；**修复 `SetPath` 分级配置：不再创建根目录汇总文件，避免日志同时出现在根目录和子目录**
- `lg/logger_test.go`：保留 `TestModule_FollowsSetDefault` 验证子 Logger 跟随行为；`TestSetPath_WithLevelDir` 新增验证根目录无日志文件
- `cmd/pg/main.go`：`initLogging()` 改用 `WithLevelDir` 按级别分目录存储（info/error）；`"Pi Agent"` → `"Pi-Go Agent"`（system prompt、help）
- `tui/terminal.go`：新增 `StripANSI()` 函数；新增 `BgCard()` 函数（后删除）；修复光标定位：`fullRender`/`diffRender` 末尾改用 `CursorBack(len(line) - curCol)` 替代 `CursorForward(curCol)`，确保光标从文本末尾正确移到 curCol 位置；`renderLine` 光标位置字符反色显示（`\033[7m`）
- `tui/box.go`：保留 Unicode 框线工具（备用，未使用）
- `tui/markdown.go`：修复 `renderInline` 标题处理 bug，`### 标题` 之前未去掉 `### ` 前缀导致用户看到裸 Markdown；改为 `Bold(strings.TrimPrefix(trimmed, "### "))`
- `tui/chat.go`：`printWelcome()` 中 `"Pi Agent"` → `"Pi-Go Agent"`；`Run()` 中去掉 `renderUserBox`（LineEditor 已实时回显）；`renderAgentStream` 保持流式输出，每行实时输出，无背景色；工具调用/结果/tokens 缩进 2 空格对齐
- `tui/chat_test.go`：测试调用 `renderAgentBox` → `renderAgentStream`
